services:
  - type: web
    name: translatewebjp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn mercari_api_server:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
